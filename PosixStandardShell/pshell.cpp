#include "pshell.hpp"

// Start the shell by getting user inputs
void start_shell() {
    
}

int main()
{
    std::cout << std::unitbuf;
    std::cerr << std::unitbuf;

    start_shell();

    return 0;
}